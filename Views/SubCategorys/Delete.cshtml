@model Project_497.Models.SubCategoryID

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Layout/LayoutPage.cshtml";
    if (Session["UserId"] == null)
    {
        Response.Redirect("~/Login/Index");
    }
}

<h2 style="font-weight:200; font-size:large">Delete</h2>

<h3 style="font-weight:200; font-size:large">Are you sure you want to delete this?</h3>
<div>
    <h4 style="font-weight:200; font-size:large">SubCategoryID</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt style="font-weight:200; font-size:large;color:forestgreen">
            @Html.DisplayNameFor(model => model.name)
        </dt>

        <dd style="font-weight:700; font-size:large;color:#0094ff">
            @Html.DisplayFor(model => model.name)
        </dd>

        <dt style="font-weight:200; font-size:large;color:forestgreen">
            @Html.DisplayNameFor(model => model.category.name)
        </dt>

        <dd style="font-weight:700; padding:15px; font-size:large;color:#0094ff">
            @Html.DisplayFor(model => model.category.name)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-success"/> 
            <p style="font-size:larger; padding:15px; font-weight:900">@Html.ActionLink("Back to List", "Index")</p>
        </div>
    }
</div>
