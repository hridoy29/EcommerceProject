@using Project_497.Models
@model List<category>
<ul class="nav navbar-nav navbar-static-top">
    @{
        foreach (var item in Model)
        {
           
            <li>@Html.ActionLink(@item.name, "Index", "Product", new { page = 1, category = item.categoryId }, null)</li>
            
        }

    }

</ul>

